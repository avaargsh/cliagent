# QA 报告 · 2026-04-01-v2（回归验收）

## 1. 测试范围

- 本轮是 `qa-report-20260401.md` 的回归验收，聚焦确认 4 个 P0 阻塞项是否修复。
- 覆盖范围：
  - `config show|validate`
  - `employee list|show|test`
  - `work-order create|get|list|run|cancel|resume|watch|artifacts|reclaim`
  - `session list|get|tail|export`
  - `approval list|get|decide`
  - `tool list|show|dry-run`
  - `doctor`
  - `replay`（最小闭环）
  - `mock provider`
  - 协调器路径（`--coordinated`）
  - 后台执行与心跳
  - 租户隔离（CLI 集成级别）
  - `unittest` 回归基线（87 测试）
- 本轮未覆盖：
  - REST 入口
  - `openai` 真实 provider 联调
  - 跨机器文件锁场景

## 2. P0 / P1 验收结果

| 级别 | 总数 | 通过 | 失败 | 结论 |
|---|---|---|---|---|
| P0 | 7 | 7 | 0 | 通过 |
| P1 | 1 | 1 | 0 | 通过 |

前次 P0 失败项回归结果：

| 编号 | 原始现象 | 回归结果 | 验证方式 |
|---|---|---|---|
| QA-001 | 输入文件不存在时输出 traceback | **已修复** — 返回 `exit 2`、`input_file_unreadable`、无 traceback | CLI 手动验证 + `test_input_file_error_maps_to_exit_2_json/human` |
| QA-002 | `config validate` 对非法配置返回成功 | **已修复** — 返回 `exit 3`、`config_invalid`、`ok=false` | CLI 手动验证 + `test_config_validate_allowed_on_bad_config` |
| QA-003 | 非法配置不阻断业务命令 | **已修复** — `employee list` 等命令被阻断，`exit 3` | CLI 手动验证 + `test_employee_list_blocked_on_bad_config` |
| QA-004 | 租户隔离缺失 | **已修复** — `tenant-b` 无法看到 `tenant-a` 工单 | CLI 手动验证 + `test_tenant_b_cannot_see_tenant_a_work_orders`（新增） |

## 3. 本轮新发现缺陷

无新增 P0/P1 缺陷。

## 4. 本轮额外修复

| 项目 | 说明 |
|---|---|
| YAML null section 崩溃 | `system.yaml` 中 `runtime:` 无子键时 `load_app_config` 抛 `AttributeError`。修复：`system_payload.get("runtime") or {}`。新增单测覆盖。 |

## 5. 未覆盖场景

- REST 入口端到端验收
- `openai` 真实 provider 联调
- 文件仓储跨进程并发压力测试
- 跨机器 / NFS 文件锁场景
- Windows 平台（`fcntl` 不可用）

## 6. 已知限制

- 环境缺少 `pydantic`、`fastapi`、`pytest`，当前实现使用 `dataclasses + argparse + unittest`
- REST 入口仅为懒加载占位，未纳入本轮验收
- `--profile` 参数仅记录，不改变实际配置加载行为
- 文件仓储使用 `fcntl` advisory lock，仅在 Unix 同机器多进程场景下有效

## 7. 发布建议

**Go**

原因：

- 4 个 P0 阻塞项全部修复并有自动化测试覆盖
- 87 个测试全部通过
- 控制面协议（exit code、JSON 包裹、错误类型）已稳定
- 租户隔离在仓储和 CLI 两个层面均已验证
- 无新增 P0/P1 缺陷

建议后续版本关注：

1. REST 入口的端到端验收
2. Windows 平台兼容性（文件锁替代方案）
3. `openai` provider 真实联调
4. `--profile` 分层配置的落地或文档说明
