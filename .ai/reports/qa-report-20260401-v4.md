# QA 报告 · 2026-04-01-v4（产品化推进回归验收）

## 1. 测试范围

- 本轮基于 `review-report-20260401-v5.md` 做回归验收。
- 当前仓库仍缺少正式 `.ai/temp/requirement.md`，本轮按“回归验收 + 当前迭代目标”执行。
- 覆盖范围：
  - `replay run` 新增主链
  - `approval -> resume --background` 稳定性回归
  - 文件仓储并发一致性改动回归
  - 全量 `unittest` 回归基线（`98` 测试）
  - CLI 全局参数顺序文档与实现一致性
- 本轮未覆盖：
  - 真实 OpenAI API 联调
  - REST 入口端到端
  - Windows 平台
  - 跨机器 / NFS 文件锁场景

## 2. P0 / P1 验收结果

| 级别 | 总数 | 通过 | 失败 | 结论 |
|---|---|---|---|---|
| P0 | 4 | 4 | 0 | 通过 |
| P1 | 2 | 2 | 0 | 通过 |

## 3. 本轮新发现缺陷

无新增 P0/P1 缺陷。

## 4. 关键结果摘要

- `tests.integration.cli.test_replay_commands`：
  - `Ran 2 tests`
  - `OK`
- `test_approval_decide_can_auto_resume_background`：
  - 单次 `OK`
  - 循环 20 次：`failures=0/20`
- 全量回归：
  - `python3 -m unittest discover -s tests -p 'test_*.py'`
  - `Ran 98 tests`
  - `OK`
- 稳定性回归：
  - 全量连续 5 轮
  - `total_failures=0/5`

## 5. 缺陷统计

| 类别 | 数量 |
|---|---|
| P0 缺陷 | 0 |
| P1 缺陷 | 0 |
| 产品缺陷 | 0 |
| 体验问题 | 0 |
| 技术债 | 0 |

## 6. 未覆盖场景

- 真实 OpenAI API 联调
- OpenAI 路径审批后的恢复主链端到端
- REST 入口端到端
- 跨机器 / NFS 文件锁
- Windows 平台兼容性

## 7. 已知限制

- 默认员工画像仍以 `mock` provider 为主；`openai` 以自动化测试回归为主
- `--profile` 当前仅记录，不改变配置加载行为
- 打包链路依赖本地安装 `build` 模块（本轮未做构建验收）

## 8. 发布建议

**Go**

原因：

- 新增能力（`replay run`）已通过自动化与集成回归
- 后台恢复链路稳定性经循环验证通过
- 全量测试基线 `98` 且短周期稳定
- 无 P0/P1 缺陷
